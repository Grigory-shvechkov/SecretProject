# opencv-python is deliberately NOT listed here for pip to install.
# On this Pi (Python 3.9), pip's source build fails patching OpenEXR --
# there's no matching prebuilt wheel for this platform/Python combo.
# Install it via apt instead, which ships a precompiled binary:
#     sudo apt install -y python3-opencv
numpy
requests

# ultralytics (YOLO, used by Vision/detection.py's FishDetector) and its
# own dependencies (torch, etc.) are deliberately NOT listed here either,
# same reasoning as opencv-python above plus one more failure mode: pip's
# resolver trying to satisfy ultralytics's pinned dependencies against
# what's already installed can fall into backtracking through every past
# ultralytics release one at a time looking for a combination that
# resolves -- this can churn for a very long time instead of just failing
# fast. See requirements-ml.txt (install with --no-deps) for that stack.
#
# torch/torchvision in particular usually need a Pi-specific wheel from
# https://www.piwheels.org rather than plain PyPI -- if pip can't find a
# wheel at all for this Pi's architecture/Python version, that's a piwheels
# coverage gap, not a resolver problem, and needs a different fix (e.g. an
# older torch release, or building from source).
